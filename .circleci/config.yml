version: 2.0

jobs:
    build:
        docker:
            - image: base/archlinux
        working_directory: ~/dotfiles
        steps:
            - run: pacman -S git
            - checkout
            - run: git submodule update --init
