extends: default

rules:
  document-start: disable
  truthy: disable
  quoted-strings:
    quote-type: double
    required: only-when-needed
  line-length:
    max: 150

ignore: |
  galaxy_roles
  galaxy_collections
