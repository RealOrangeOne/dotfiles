# Dotfiles

Settings and Preferences for transfer between machines

## Requirements
- SSH / GPG keys installed
- Correctly setup `~/.dotfiles` directory
- Ansible
- `just`

## Usage
1. Run `just setup` to setup local environment
2. Run `just deploy`
3. ???
4. Profit!
