- name: Install NTP
  aur:
    name: "{{ item }}"
  become: true
  become_user: aur_builder
  loop:
    - ntp

- name: Set timezone
  timezone:
    name: Europe/London

- name: Enable NTP service
  systemd:
    name: ntpd
    enabled: true
    state: started
