- name: Make me
  user:
    name: "{{ user }}"
    home: "{{ home }}"
    comment: Jake Howard
    shell: '/usr/bin/zsh'
    uid: 1000
    system: true
    groups:
      - input
      - users
      - video
      - wheel

- name: Give user sudo access
  lineinfile:
    path: /etc/sudoers
    line: "{{ user }} ALL=(ALL) ALL"
